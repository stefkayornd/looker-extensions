import { flexAdminThemeOptions, hybridAdminThemeOptions, RndThemeOptions } from '@officernd/core-ui'

export const APP_THEMES: { [key: string]: RndThemeOptions } = {
  Flex: flexAdminThemeOptions,
  Hybrid: hybridAdminThemeOptions,
  Default: flexAdminThemeOptions,
}
